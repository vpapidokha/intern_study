dependencies {
   compile 'joda-time:joda-time:2.3'
}

jar {
  manifest {
    attributes(
      'Main-Class': 'com.example.SystemOut'
      )
  }
  from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  }
}
